#extend("base")

#export("title") { #(event.name) }

#export("content") {

    <h2>#(event.name)</h2>

    #embed("Fragments/event-menu")

    #loop(submissions, "submission") {
        <li>#(submission.problem.name)</li>
    }
}
